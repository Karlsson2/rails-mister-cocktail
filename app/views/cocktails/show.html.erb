<h1><%=@cocktail.name %></h1>
<% @cocktail.doses.each do |dose|%>
<p><%= dose.description%> of <%= dose.ingredient.name %></p>

<% end %>
<%= link_to "Add ingredient", cocktails_path(), class: "btn btn-primary" %>
<%= link_to "back", cocktails_path(), class: "btn btn-primary" %>
